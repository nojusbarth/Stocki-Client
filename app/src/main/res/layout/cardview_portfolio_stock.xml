<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cvStockItem"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    app:strokeColor="@android:color/darker_gray"
    app:strokeWidth="1dp"
    android:clickable="true"
    android:foreground="?attr/selectableItemBackground"
    android:backgroundTint="@color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <!-- Ticker + Quantity + Price -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <LinearLayout
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/txtPortfolioStockTicker"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="AAPL"
                    android:textSize="20sp"
                    android:textStyle="bold"
                    android:textColor="@color/black"/>

                <TextView
                    android:id="@+id/txtPortfolioStockQuantityPrice"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="10 shares @ $150"
                    android:textSize="14sp"
                    android:textColor="@android:color/darker_gray"/>
            </LinearLayout>

            <!-- Aktuelle Preisangabe -->
            <TextView
                android:id="@+id/txtPortfolioStockAbsValue"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="$1500"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="@color/black"/>
        </LinearLayout>

        <!-- Rendite-Zeile mit pctReturn, absReturn und totalBuyIn -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="8dp"
            android:weightSum="3">

            <TextView
                android:id="@+id/txtPortfolioStockPctReturn"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="+2.5%"
                android:textSize="16sp"
                android:textColor="@color/green"/>

            <TextView
                android:id="@+id/txtPortfolioStockAbsReturn"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="+$50"
                android:textSize="16sp"
                android:gravity="center"
                android:textColor="@color/green"/>

            <TextView
                android:id="@+id/txtPortfolioStockBuyIn"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="Buy-in: $1500"
                android:textSize="16sp"
                android:gravity="end"
                android:textColor="@android:color/darker_gray"/>
        </LinearLayout>


        <!-- Aktion-Icons: Buy, Sell, Info -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="end"
            android:layout_marginTop="8dp"
            android:spacing="8dp">

            <ImageButton
                android:id="@+id/btnPortfolioBuyStock"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:src="@drawable/baseline_add_24"
                android:contentDescription="Buy"
                app:tint="@color/green"
                android:background="?attr/selectableItemBackgroundBorderless"/>

            <ImageButton
                android:id="@+id/btnPortfolioSellStock"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:src="@drawable/baseline_attach_money_24"
                android:contentDescription="Sell"
                app:tint="@color/red"
                android:background="?attr/selectableItemBackgroundBorderless"/>

            <ImageButton
                android:id="@+id/btnPortfolioInfoStock"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:src="@android:drawable/ic_menu_info_details"
                android:contentDescription="Info"
                app:tint="@color/light_gray"
                android:background="?attr/selectableItemBackgroundBorderless"/>
        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
